import PathForm from "@/app/component/Pathform"; // Importer le formulaire

export default function Home() {
  return (
    <main className="min-h-screen flex items-center justify-center bg-gray-100">
      <PathForm />
    </main>
  );
}